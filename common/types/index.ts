export enum BannerStyleType {
  AD = 'AD',
  SECTION = 'SECTION',
}

export enum ButtonType {
  COMPLETE = 'COMPLETE',
  LOAD = 'LOAD',
  SELECTION = 'SELECTION',
}

export enum ButtonSize {
  LONG = 'LONG',
  SHORT = 'SHORT',
}
